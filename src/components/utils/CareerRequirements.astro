---
--- 
<!-- Contenido de Requisitos con iconos y QR -->
<section id="career-requirements" class="bg-gradient-to-r from-gray-50 to-gray-100 py-16">
    <div class="container max-w-4xl mx-auto px-4">
      <h2 class="mb-12 text-center text-4xl font-bold text-gray-800">Requisitos para Inscribirte</h2>
  
      <!-- Selector de Carrera -->
      <div class="mb-8 flex justify-center">
        <div class="relative inline-block text-left">
          <select id="career-select" class="pl-4 pr-10 py-2 border border-gray-300 rounded-lg shadow-sm">
            <option disabled selected>Selecciona una carrera:</option>
            <option value="ingenieria">Ingeniería de Sistemas</option>
            <option value="derecho">Derecho</option>
            <option value="administracion">Administración de Empresas</option>
            <option value="auditoria">Auditoría</option>
            <option value="medicina">Medicna</option>
            <option value="odontologia">Odontología</option>
            <option value="enfermeria">Enfermería</option>
            <option value="gastronomia">Gastronomía, Turismo y Hotelería</option>
          </select>
        </div>
      </div>
  
      <!-- Contenido interactivo de requisitos -->
      <div id="requirements-content" class="grid gap-8 md:grid-cols-2 transition-opacity duration-300">
        <!-- Documentación Básica -->
        <div class="bg-white p-6 shadow-xl rounded-lg transform hover:scale-105 transition-transform duration-300">
          <h3 class="text-2xl font-semibold text-gray-800 mb-4 flex items-center">
            <svg class="h-6 w-6 mr-2 text-red-600 transition-transform duration-300 hover:rotate-12" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 6H7a2 2 0 01-2-2V7a2 2 0 012-2h5l2 2h5a2 2 0 012 2v12a2 2 0 01-2 2z" />
            </svg>
            Documentación Básica
          </h3>
          <ul class="list-disc list-inside text-gray-600">
            <li>Fotocopia legalizada del Título de Bachiller</li>
            <li>Certificado de nacimiento original</li>
            <li>Fotocopia de la C.I.</li>
            <li>2 fotografías 4x4 y 2 tamaño carnet (fondo rojo)</li>
            <li>Baucher de depósito bancario</li>
          </ul>
        </div>
  
        <!-- Carpetas -->
        <div class="bg-white p-6 shadow-xl rounded-lg transform hover:scale-105 transition-transform duration-300">
          <h3 class="text-2xl font-semibold text-gray-800 mb-4 flex items-center">
            <svg class="h-6 w-6 mr-2 text-red-600 transition-transform duration-300 hover:rotate-12" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v4a2 2 0 002 2h14a2 2 0 002-2V7m-2-4H5a2 2 0 00-2 2v0a2 2 0 002 2h14a2 2 0 002-2v0a2 2 0 00-2-2z" />
            </svg>
            Carpetas
          </h3>
          <ul id="folder-text" class="list-disc list-inside text-gray-600">
            <li>Folder amarillo con acofaster (para todas las carreras)</li>
          </ul>
          <p class="mt-4 text-sm text-gray-500">*En caso de no contar con toda la documentación, tienes 15 días para regularizar.</p>
        </div>
  
        <!-- Métodos de Pago -->
        <div class="bg-white p-6 shadow-xl rounded-lg md:col-span-2 transform hover:scale-105 transition-transform duration-300">
          <h3 class="text-2xl font-semibold text-gray-800 mb-4 flex items-center">
            <svg class="h-8 w-8 mr-2 text-red-600 transition-transform duration-300 hover:rotate-12" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z" />
            </svg>
            Métodos de Pago
          </h3>
          <div class="md:flex md:items-center">
            <div class="md:w-1/2">
              <h4 class="text-lg font-semibold text-gray-800 mb-2">Pago en efectivo:</h4>
              <p class="text-gray-600 mb-4">
                Realiza tu depósito en el banco BNB al número de cuenta <span id="bank-account">500-0050530</span>.
              </p>
              <h4 class="text-lg font-semibold text-gray-800 mb-2">Pago en oficina:</h4>
              <p class="text-gray-600">
                Puedes pagar directamente en nuestras oficinas utilizando:
              </p>
              <ul class="list-disc list-inside text-gray-600 mt-2">
                <li>Tarjeta de débito o crédito.</li>
                <li>Transferencia bancaria.</li>
                <li>Escaneando nuestro código QR.</li>
              </ul>
            </div>
  
            <!-- Imagen QR con esquinas redondeadas -->
            <!-- Imagen QR -->
            <div class="flex justify-center mt-6 md:mt-0 md:w-1/2">
                <img
                src="/carreras/qr/qr-code.jpeg"
                alt="QR para Pago"
                id="qr-image"
                class="h-64 w-auto object-contain rounded-xl cursor-pointer transition-transform hover:scale-110"
                />
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
  
  <!-- Modal del QR -->
  <div id="qr-modal" class="fixed inset-0 hidden bg-black bg-opacity-75 items-center justify-center z-50">
    <img id="expanded-qr" alt="QR Ampliado" class="max-w-[90%] max-h-[90%] object-contain" />
  </div>
  
  <!-- Script de interactividad -->
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      // Elementos con type assertion para obtener el tipo correcto
      const careerSelect = document.getElementById('career-select') as HTMLSelectElement;
      const bankAccount = document.getElementById('bank-account');
      const qrImage = document.getElementById('qr-image') as HTMLImageElement;
      const qrModal = document.getElementById('qr-modal') as HTMLDivElement;
      const expandedQR = document.getElementById('expanded-qr') as HTMLImageElement;
  
      // Cambia los detalles de pago según la carrera seleccionada
      careerSelect.addEventListener('change', () => {
        if (careerSelect.value === 'gastronomia') {
          bankAccount.textContent = '500-0062245';
          qrImage.src = '/carreras/qr/qr-code-gastronomia.jpeg';
        } else {
          bankAccount.textContent = '500-0050530';
          qrImage.src = '/carreras/qr/qr-code.jpeg';
        }
      });
  
      // Mostrar el modal del QR al hacer clic en la imagen
      qrImage.addEventListener('click', () => {
        expandedQR.src = qrImage.src;
        qrModal.classList.remove('hidden');
        qrModal.classList.add('flex');
      });
  
      // Ocultar el modal al hacer clic en cualquier parte del mismo
      qrModal.addEventListener('click', () => {
        qrModal.classList.add('hidden');
      });
    });
  </script>
  
  <style>
    /* Transición para el contenido */
    #requirements-content {
      transition: opacity 0.3s ease-in-out;
    }
  
    /* Estilo del modal */
    #qr-modal {
      backdrop-filter: blur(5px);
      transition: opacity 0.3s ease;
    }
  </style>
  
  