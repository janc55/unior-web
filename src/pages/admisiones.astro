---
import Header from '../components/Header.astro';
import Layout from '../layouts/Layout.astro';
import { Icon } from 'astro-icon/components';
import CareerRequirements from '../components/utils/CareerRequirements.astro';

const admissionSteps = [
  {
    step: 1, // Número de paso
    title: "Solicita Información",
    description: "Explora nuestras carreras y obtén todos los detalles y requisitos de inscripción. ¡Estamos aquí para responder todas tus dudas!",
    icon: "mdi:email-outline"
  },
  {
    step: 2, // Número de paso
    title: "Reúne tus Requisitos",
    description: "Prepara los documentos necesarios como el Título de Bachiller, certificado de nacimiento y más. ¡Recuerda que tienes 15 días para regularizar cualquier documento faltante!",
    icon: "mdi:file-document-outline"
  },
  {
    step: 3, // Número de paso
    title: "Realiza el Pago",
    description: "Elige tu forma de pago favorita: en efectivo, mediante QR, transferencia bancaria o tarjeta. ¡Es súper flexible!",
    icon: "mdi:cash-check"
  },
  {
    step: 4, // Número de paso
    title: "Presenta tus Documentos",
    description: "Entrega tus documentos junto con el baucher de pago. Esta es la etapa oficial de inscripción.",
    icon: "mdi:folder-account"
  },
  {
    step: 5, // Número de paso
    title: "Asignación de Materias",
    description: "¡Ya casi eres parte de la comunidad! Ahora solo te queda la asignación de materias para iniciar tu semestre.",
    icon: "mdi:calendar-check"
  }
];

const faqs = [
  {
    question: "¿Cuándo comienzan las inscripciones?",
    answer: "Las inscripciones para el próximo semestre comienzan el 29 de noviembre y finalizan el 24 de febrero."
  },
  {
    question: "¿Qué documentos necesito para inscribirme?",
    answer: "Necesitarás una fotocopia legalizada de tu título de bachiller, certificado de nacimiento, fotocopia simple de tu cédula de identidad, 2 fotografías tamaño carnet, 2 fotgrafias 4x4 en fondo rojo y el comprobante de pago de la matrícula."
  },
  {
    question: "¿Ofrecen becas o ayuda financiera?",
    answer: "Sí, ofrecemos diversas becas por mérito académico, deportivo y necesidad económica. Consulta nuestra sección de becas para más información."
  },
  {
    question: "¿Puedo cambiar de carrera una vez inscrito?",
    answer: "Sí, es posible cambiar de carrera durante las primeras dos semanas del semestre, sujeto a disponibilidad de cupos."
  }
];
---

<Layout title="Admisiones - Universidad Privada de Oruro">
  <Header isHero={false}/>
  <main>
    
    <!-- Proceso de Admisión -->
    <section class="py-4">
      <div class="container max-w-[1200px] mx-auto px-4">
        <h2 class="mb-12 text-center text-3xl font-bold text-primary animate-fade-in-down">¡Inscribirte es fácil!</h2>
        <p class="text-center text-gray-600 mb-8">Sigue estos sencillos pasos para comenzar tu futuro en nuestra universidad. Si tienes dudas, ¡no dudes en contactarnos!</p>
        <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
          {admissionSteps.map((step, index) => (
            <div class="group rounded-lg bg-white p-6 shadow-lg transition-all duration-300 hover:shadow-xl animate-fade-in-up" style={`animation-delay: ${index * 100}ms`}>
              <!-- Contenedor del ícono con número de paso -->
              <div class="relative mb-4 flex h-16 w-16 items-center justify-center rounded-full bg-primary text-white transition-all duration-300 group-hover:bg-yellow-400">
                <!-- Número de paso -->
                <span class="absolute -top-2 -right-2 flex h-8 w-8 items-center justify-center rounded-full bg-yellow-400 text-sm font-bold text-gray-800 transition-all duration-300 group-hover:bg-primary group-hover:text-white">
                  {step.step}
                </span>
                <!-- Ícono -->
                <Icon name={step.icon} class="h-8 w-8 transition-all duration-300 group-hover:text-gray-800" />
              </div>
              <!-- Título y descripción -->
              <h3 class="mb-2 text-xl font-semibold text-gray-800">{step.title}</h3>
              <p class="text-gray-600">{step.description}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Sección de Requisitos interactiva (sin React) -->
    <CareerRequirements />

    <!-- CTA Inscripción -->
    <section class="bg-gray-200 py-16">
      <div class="container max-w-[1200px] mx-auto px-4 text-center">
        <h2 class="text-3xl font-bold text-primary mb-6">
          ¡Inscríbete hoy mismo!
        </h2>
        <p class="text-gray-600 mb-6">
          Si ya cuentas con todos los requisitos, da el primer paso hacia tu futuro. Haz clic en el botón y completa tu inscripción en línea.
        </p>
        <a
          href="https://inscripciones.unior.edu.bo"
          class="inline-block rounded-full bg-primary px-8 py-3 text-lg font-semibold text-white transition-colors hover:bg-primary-dark"
        >
          Inscripción en Línea
        </a>
      </div>
    </section>

    <!-- Preguntas Frecuentes -->
    <section class="py-16">
      <div class="container max-w-[1200px] mx-auto px-4">
        <h2 class="mb-12 text-center text-3xl font-bold text-primary">Preguntas Frecuentes</h2>
        <div class="grid gap-6 md:grid-cols-2">
          {faqs.map((faq, index) => (
            <div
              class="rounded-lg bg-white p-6 shadow-lg animate-fade-in-up"
              style={`animation-delay: ${index * 100}ms`}
            >
              <h3 class="mb-2 text-xl font-semibold text-gray-800">{faq.question}</h3>
              <p class="text-gray-600">{faq.answer}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Sección de Contacto -->
    <section class="bg-gray-100 py-16">
      <div class="container max-w-[1200px] mx-auto px-4">
        <h2 class="mb-12 text-center text-3xl font-bold text-primary">
          ¿Tienes más preguntas?
        </h2>
        <div class="rounded-lg bg-white p-8 shadow-lg md:flex md:items-center md:justify-between">
          <div class="mb-6 md:mb-0 md:w-2/3">
            <p class="text-gray-600">
              Nuestro equipo de admisiones está listo para guiarte en cada paso del proceso.
            </p>
          </div>
          <div class="md:w-1/3 md:text-right">
            <a
              href="https://wa.link/bdiw3q"
              class="inline-flex items-center rounded-full bg-primary px-6 py-3 text-lg font-semibold text-white transition-colors hover:bg-primary-dark"
            >
              <Icon name="mdi:phone" class="mr-2 h-5 w-5" />
              Contáctanos
            </a>
          </div>
        </div>
      </div>
    </section>
  </main>
</Layout>

<style>
  @keyframes fadeInDown {
    from {
      opacity: 0;
      transform: translate3d(0, -20px, 0);
    }
    to {
      opacity: 1;
      transform: translate3d(0, 0, 0);
    }
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translate3d(0, 20px, 0);
    }
    to {
      opacity: 1;
      transform: translate3d(0, 0, 0);
    }
  }

  .animate-fade-in-down {
    animation: fadeInDown 1s ease-out;
  }

  .animate-fade-in-up {
    animation: fadeInUp 1s ease-out;
  }

  [astro-icon] {
    color: currentColor;
  }
</style>

