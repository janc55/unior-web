---
import Layout from '../../layouts/Layout.astro';
import { Image } from 'astro:assets';
import OptimizedVideo from '../../components/utils/video-bfl.astro';
import Header from '../../components/Header.astro';

const careers = [
  { id: 1, name: 'Odontología', details: 'Acreditada al MERCOSUR Educativo', href: '/carreras/odontologia' },
  { id: 2, name: 'Medicina', href: '/carreras/medicina' },
  { id: 3, name: 'Enfermería', href: '/carreras/enfermeria' },
  { id: 4, name: 'Gastronomía, Turismo y Hotelería', href: '/carreras/gastronomia-turismo-hoteleria' },
  { id: 5, name: 'Derecho', href: '/carreras/derecho' },
  { id: 6, name: 'Administración de Empresas', href: '/carreras/administracion-empresas' },
  { id: 7, name: 'Auditoría', href: '/carreras/auditoria' },
  { id: 8, name: 'Ingeniería de Sistemas', href: '/carreras/ingenieria-de-sistemas' },
];

// Reemplaza esta URL con la URL real de tu formulario de Google Forms
const googleFormsUrl = "https://forms.gle/j3SmHueWSaCvHzfx6";
---


<Layout title="BECA FUTURO LÍDER - UNIOR">
  <Header />
  <main class="min-h-screen bg-gradient-to-br from-gray-900 via-gray-800 to-gray-700 text-white">
    <div class="container max-w-[1440px] mx-auto px-4 py-12">
      <h1 class="text-4xl md:text-6xl font-bold text-center mb-8 animate-fade-in-down">
        BECA FUTURO LÍDER
      </h1>
      <p class="text-xl md:text-2xl text-center mb-12 animate-fade-in-up">
        ¡Una oportunidad única para tu futuro profesional!
      </p>

      <div class="grid md:grid-cols-2 gap-12 items-center mb-16">
        <div class="space-y-6 animate-fade-in-left">
          <h2 class="text-3xl font-semibold">¿Qué ofrecemos?</h2>
          <ul class="list-disc list-inside space-y-2">
            <li>Beca excepcional del 40% para toda tu carrera</li>
            <li>Oportunidad de estudiar en la universidad líder en calidad educativa</li>
            <li>Requisitos simples: mantén un buen promedio</li>
          </ul>
          <a href={googleFormsUrl} target="_blank" rel="noopener noreferrer" class="inline-block bg-gradient-to-r from-red-600 via-red-500 to-orange-500 text-white font-bold py-3 px-6 rounded-full hover:from-orange-600 hover:to-red-600 transition duration-300 transform hover:scale-105">
            Postula Ahora
          </a>
        </div>
        <div class="relative animate-fade-in-right">
          <OptimizedVideo
            webmSrc="/bfl.webm"
            mp4Src="/bfl.mp4"
            posterSrc="/image2.png"
          />
          <div class="absolute -bottom-4 -right-4 bg-gradient-to-br from-red-600 via-red-500 to-orange-500 p-4 rounded-lg shadow-xl">
            <p class="text-2xl font-bold">40% de Beca</p>
            <p>Durante todos tus estudios</p>
          </div>
        </div>
      </div>

      <section class="mb-16">
        <h2 class="text-3xl font-semibold text-center mb-8">Elige tu camino profesional</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
          {careers.map((career) => (
            <a
              href={career.href}
              class="bg-white bg-opacity-10 p-6 rounded-lg shadow-xl hover:bg-opacity-20 transition duration-300 transform hover:scale-105"
            >
              <h3 class="text-xl font-semibold mb-2">{career.name}</h3>
              {career.details && <p class="text-sm text-red-500">{career.details}</p>}
            </a>
          ))}
        </div>
      </section>

      <section id="postular" class="bg-white bg-opacity-10 p-8 rounded-lg shadow-xl text-center">
        <h2 class="text-3xl font-semibold mb-8">Postula a la Beca Futuro Líder</h2>
        <p class="mb-6">Completa nuestro formulario de postulación y da el primer paso hacia tu futuro profesional.</p>
        <a href={googleFormsUrl} target="_blank" rel="noopener noreferrer" class="inline-block bg-gradient-to-r from-red-600 via-red-500 to-orange-500 text-white font-bold py-3 px-6 rounded-full hover:from-orange-600 hover:to-red-600 transition duration-300 transform hover:scale-105">
          Ir al Formulario de Postulación
        </a>
      </section>
    </div>
  </main>
</Layout>

<style>
  @keyframes fadeInDown {
    from { opacity: 0; transform: translateY(-20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes fadeInLeft {
    from { opacity: 0; transform: translateX(-20px); }
    to { opacity: 1; transform: translateX(0); }
  }
  @keyframes fadeInRight {
    from { opacity: 0; transform: translateX(20px); }
    to { opacity: 1; transform: translateX(0); }
  }
  .animate-fade-in-down { animation: fadeInDown 1s ease-out; }
  .animate-fade-in-up { animation: fadeInUp 1s ease-out; }
  .animate-fade-in-left { animation: fadeInLeft 1s ease-out; }
  .animate-fade-in-right { animation: fadeInRight 1s ease-out; }
</style>